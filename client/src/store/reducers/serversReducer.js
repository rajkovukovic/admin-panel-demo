// const { Map } = require('immutable');
import initialState from '../../__mocks__/servers';


export default function accounts(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
